# VOCODEX Backend Notes
## Deps summary
- FastAPI: the framework
- Uvicorn: the server that runs it
- SQLAlchemy + asyncpg: talk to `Postgres` in an async, non-blocking way
- Pydantic: guarantees your inputs/outputs are clean and typed
